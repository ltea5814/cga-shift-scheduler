# Google 試算表同步設定

## 1. 建立 Apps Script

1. 建立一份新的 Google 試算表。
2. 開啟「擴充功能 → Apps Script」。
3. 將 `google-apps-script/Code.gs` 的完整內容貼上，儲存。
4. 按「部署 → 新增部署作業」，類型選「網頁應用程式」。
5. 「執行身分」選自己；「誰可以存取」選「任何人」。
6. 完成授權後複製 `/exec` 結尾的網頁應用程式網址。

## 2. 連接排班網頁

在排班網頁的「備份與說明 → Google 試算表同步」貼上 `/exec` 網址，按「儲存網址」。

- 「從試算表載入」：以 Google 試算表資料覆蓋目前手機資料。
- 「上傳至試算表」：將目前手機資料寫回 Google 試算表。

建議流程是同事先在手機登記請假或修改資料，承辦人先按「從試算表載入」取得最新版本，確認後再按「上傳至試算表」。

程式第一次上傳後會自動建立四個工作表：`排班資料`、`人員名冊`、`請假資料`、`系統設定`。排班資料可直接在試算表中修改「人員」欄，再回網頁按「從試算表載入」。

## 3. 安全提醒

這個部署設定為「任何人」才能讓手機網頁讀寫；取得網址的人也可能讀寫這份表格。請只在辦公室內部使用，並避免把網址公開到公開網站。若需要登入控管，後續應改用 Google OAuth 或 Google Workspace 網域限制。
